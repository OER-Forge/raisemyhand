HOST=0.0.0.0
PORT=8000
DATABASE_URL=sqlite:///./raisemyhand.db

# Base URL for generating external links (QR codes, shared URLs)
# Set this to your public URL when deploying
BASE_URL=http://localhost:8000

# Timezone for timestamp display (IANA format, e.g., America/New_York, Europe/London)
TIMEZONE=UTC

# Admin Authentication - CHANGE THESE BEFORE DEPLOYING TO PRODUCTION!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123
